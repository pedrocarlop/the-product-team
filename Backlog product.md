# **Backlog**
update skills
- ux designer
- content
- engineering todas
- business todas

cambiar que el orchestrator cuando asigne rols pregunte si tiene el subagente todo lo necesario para trabjar, entonces el agente cuando lee las skills, si no hay ninguna hta set up, en vez de fallback siempre devuelve al orchestrator que no tiene htas set up y que le pregunte al prompter si quiere set un alguna de las dispones en las skills os si prefiere fallback. el prompter puede incluir api keys, links etc para dejarlo todo listo o preferir no hacer nada. el orchestador debe parar el proceso para hacer todo este setup antes de continuar